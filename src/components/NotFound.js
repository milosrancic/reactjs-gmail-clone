import React from "react";

const style = {
  margin: "100px auto",
  textAlign: "center",
  fontSize: "30px"
}

const NotFound = () => ( <p style={style}>Sorry, but the page you've requested has not been found!</p> )

export default NotFound;