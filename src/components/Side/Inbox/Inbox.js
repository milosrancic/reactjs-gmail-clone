import React from 'react';
import Layout from '../../Layout';
import Hoc from "../../Hoc";

const Inbox = (props) => {
  return (
    <Hoc>
      <p>Inbox this is inbox</p>
    </Hoc>
  )
}

export default Inbox;